from typing import Union

from pydantic import BaseModel


class {{ mod }}Base(BaseModel):
    name: str


class {{ mod }}Create({{ mod }}Base):
    description: Union[str, None] = None


class {{ mod }}({{ mod }}Base):
    id: int
    description: Union[str, None] = None
    created_at: str

    class Config:
        orm_mode = True